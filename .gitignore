.env
.env.production
.env.production.local
secrets/
backups/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.hypothesis/

node_modules/
.next/
dist/
.wrangler/
playwright-report/
test-results/

.dagster/
mlruns/
mlartifacts/
artifacts/models/*.txt
artifacts/models/*.joblib
artifacts/embeddings/
artifacts/serving/
# Superseded unpinned full-catalogue vector artifact; the revision-pinned
# buildcores-full-embeddings-pinned artifact is the reproducible release input.
artifacts/retrieval/buildcores-full-embeddings/

data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
tmp/

*.db
*.sqlite
*.sqlite3
.DS_Store
Thumbs.db

# Local data drops and scratch space. Only reviewed, content-addressed evidence
# under artifacts/ is ever committed; anything staged locally stays out.
scratch/
*.tmp
*.log
*.csv.gz
*.jsonl.gz
data/**/*.npy
data/**/*.parquet
.ipynb_checkpoints/

# Credentials and keys must never be tracked; the signed-policy workflow keeps
# them outside the repository.
.env.local
.env.*.local
*.pem
*.key
*.p12
*.pfx
id_rsa*

# Editor and local tooling state
.idea/
.vscode/
*.swp
.claude/
.codex/
.aider*
