# Benchmark notes (scratch)

Blender Open Data: score is `samples/minute`, higher is better. Do NOT invert it.

Cohort identity has to include benchmark version, scene, backend and OS or the
rows are not comparable. Group splits go on product family, not on row id.

Folded into docs/model-card.md.
